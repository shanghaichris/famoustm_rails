<% title @category.name %>

<% content_for :submenu do %>
	<!--category list-->
	<%= render :partial => 'shared/categories' %>

<% end %>

<% if user_signed_in? %>
<div id="admin_category" style="display:block; margin:10px;">
	[<%= link_to t('category.edit'), edit_category_path(@category) %>][<%= link_to t('category.delete'), category_path(@category), :method => :delete,:confirm => "This will also delete all the trademarks under the category:#{@category.name}! Are you sure?" %>]
</div>
<% end %>



<!--trademark list-->
<%= render :partial => 'shared/tm_list' %>

